### 一、单项选择题（每题 1 分，共 10 分）
在恶意代码动态分析过程中，最容易暴露真实行为的分析方式是（ ）。

A. 静态反汇编分析

B. 字符串分析

C. 沙箱环境下运行分析

D. 特征码比对

以下哪一项不属于木马程序的典型特征（ ）。

A. 隐蔽运行

B. 自主高速扫描网络传播

C. 远程控制

D. 窃取敏感信息

在逆向分析中，使用符号执行技术的主要目的是（ ）。

A. 加快程序运行速度

B. 探索更多可能的路径

C. 隐藏程序真实逻辑

D. 绕过操作系统安全机制

攻击者通过注册与目标银行官网高度相似的域名（如使用 paypa1.com 代替 paypal.com）来诱导用户输入凭据，这种攻击手段属于（ ）。

A. 基于 URL 欺骗的网络钓鱼

B. Google Hacking

C. 鱼叉攻击

D. 水坑攻击

以下说法正确的是（ ）。

A. 渗透测试应该选择不影响业务系统正常运行的攻击方法进行测试

B. 当发现有可主动利用的安全漏洞时，渗透测试就必须停止，不得继续深入

C. 信息系统的深度渗透测试绝大部分采用“外部+黑盒渗透测试”的方式

D. 对《渗透测试报告》初稿的主要要求是，切勿表现观点主观评价，应该只罗列客观保证报告的客观性。

根据网络安全的基本需求模型，以下哪种攻击手段直接有效地破坏了系统的“可用性”？

A. 攻击者通过中间人攻击截获了用户账号密码。

B. 恶意软件篡改了数据库中的财务报表数据，导致审计结果错误。

C. 攻击者利用僵尸网络发动 UDP 洪泛式攻击，导致目标网站带宽耗尽。

D. 用户发送了一封邮件，事后否认发送过该邮件，接收方无法通过技术手段反驳。

以下关于缓冲区溢出的说法哪项是正确的？（ ）

A. 当输入数据超过缓冲区容量时会覆盖相邻内存内容，可能改变程序的执行流。

B. 缓冲区溢出攻击只能导致程序异常崩溃，无法执行恶意代码。

C. 只要是编译后的程序，就不存在缓冲区溢出漏洞。

D. 启用堆栈不可执行（NX）保护会让缓冲区溢出攻击变得更容易实现。

在进行网络嗅探时，必须将网卡设置为海星模式（混杂模式），其根本原因是什么？（ ）

A. 允许网卡解密经过 SSL/TLS 加密的数据包。

B. 强制网卡接收并传递所有经过其物理接口的数据包给 CPU。

C. 允许网卡绕过源 IP 地址本地策略路由攻击。

D. 防止网卡向局域网广播自身的 MAC 地址，实现隐身嗅探。

在局域网中间人攻击中使用 ARP 欺骗技术。该攻击利用了 ARP 协议的哪一核心设计缺陷？（ ）

A. ARP 协议使用明文传输，容易被嗅探。

B. ARP 协议是无状态的，设备会接受未经请求的 ARP 响应，并更新本地缓存。

C. ARP 协议基于 TCP 连接，可以通过序列号预测进行劫持。

D. ARP 协议需要通过 DNS 服务器进行解析，容易受到 DNS 投毒影响。

以下关于 SQL 注入漏洞的说法哪项是正确的？（ ）

A. SQL 注入通常由于应用程序未验证用户输入的数据合法性而产生。

B. 使用参数化的预编译查询语句无法防御 SQL 注入攻击。

C. SQL 注入攻击只会引起数据库错误日志，不会导致敏感数据泄露。

D. 防御 SQL 注入的唯一办法是关闭数据库服务对外提供查询。

### 二、判断题（每题 1 分，共 5 分。正确的请打“√”，错误的请打“×”）
花指令的核心作用是破坏反汇编结果的正确性，增加分析人员逆向分析的代价。（ ）

勒索病毒（Ransomware）的主要特征是窃取敏感数据后直接将其在暗网公开。（ ）

在 x86 架构中，栈（Stack）空间是从低地址向高地址方向增长的，这与堆（Heap）的增长方向一致。（ ）

APT 平移阶段主要做的是发现更多有价值的人员、数据、据点、系统。（ ）

启用静态 ARP 绑定仍然无法彻底防止 ARP 欺骗攻击的发生。（ ）

### 三、简答题（每题 3 分，共 15 分）
简述软件安全评估报告应该涉及的内容有哪些。

请简述两种函数调用方式 cdecl 与 stdcall 的不同点。

请简述 TCP 三次握手过程，并解释 SYN Flood 攻击是如何利用该过程制造拒绝服务攻击的。

在缓冲区溢出攻击中，什么是 Shellcode？为什么攻击者经常使用 “NOP Sled” 技术？

请简述远程网络入侵通常包含的各主要阶段，并概括每个阶段中攻击者的主要活动。

### 四、论述题（每题 10 分，共 20 分）
某公司发现内部服务器频繁与一个可疑的外部 IP 地址通讯，同时有多台主机出现未知进程。网络中还检测到加密流量异常增多。随后，该公司在一台员工电脑上发现了一个隐藏的远程访问木马。综合这些现象，安全团队怀疑公司正在遭受 APT 攻击。请根据上述情况分析：该起攻击可能经历了哪些步骤与技术手段？从现有线索来看有哪些 APT 攻击的典型特征？作为防御方，应该采取哪些措施来应对和阻止攻击的进一步蔓延？

某中型企业委托一家安全公司对其内部网络进行渗透测试，以评估网络安全防护能力。假设你是该渗透测试团队的负责人，请描述你将如何规划和执行此次渗透测试。说明渗透测试的主要阶段、每个阶段计划采用的技术手段，以及在测试过程中需要遵守的原则和注意事项。